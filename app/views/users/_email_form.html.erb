<%= form_for url_for('/profile'), method: :put, remote: true, url: update_user_email_path,
                   html: {id: :update_email_form} do |f| %>
  <%= f.label :email, 'Email' %>
  <%= f.text_field :email %>
  <% user.errors.each do |field, message| %>
    <p><strong><%= field %>:</strong> <%= message %></p>
  <% end %>
  <%= f.submit %>
<% end %>
