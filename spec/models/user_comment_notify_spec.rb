require 'spec_helper'

describe UserCommentNotify do
  # TODO: replace test without question
  # let(:user) { FactoryGirl.create(:user) }
  # let(:comment) {FactoryGirl.create(:comment_question, user: user)}

  # # TODO: need more sexy )
  # it 'Should create user comment notify when child comment created' do
  #   child_comment = FactoryGirl.build(:comment_question)
  #   child_comment.parent = comment
  #   child_comment.save
  #   last_notify = user.comment_notifies.last
  #   last_notify.comment.should == comment
  #   last_notify.child_comment.should == child_comment
  #   last_notify.read.should == false
  # end
end
