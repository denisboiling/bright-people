<%= semantic_form_for [:admin, @page] do |f| %>
  <%= f.inputs 'Основное' do %>
    <%= f.input :permalink %>
    <%= f.input :year, as: :select, collection: (2012..2020).to_a %>
    <%= f.input :text, input_html: { size: 10 } %>
  <% end %>
  <%= f.buttons %>
<% end %>
