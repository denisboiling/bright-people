<h1>Страница вопроса</h1>

Вопрос от пользователя: <%= @question.user.name %>
Вопрос:<br />

<%= simple_format @question.text %>

Ответы:
<div class="comments_list">
  <% @comments.each do |comment| %>
    <%= render 'shared/comment', comment: comment %>
  <% end %>
</div>

<%= render 'shared/comment_form' %>

<%= content_for :head do %>
  <script>
    var relation = 'Question';
    var relation_id = <%= @question.id %>;
    var can_create_comment = <%= can? :create, :comment %>
  </script>
<% end %>
