<div class="bright_magazine">
  <div class="magazine_control">
    <div class="title">Яркий журнал</div>
    <div class="text">
      Все что вы хотели узнать о детской жизни в Москве
    </div>
    <ul>
      <li class="green"><a rel="show_mag_all" href="">Все</a></li>
      <li class="red"><a rel="show_mag_article" href="">Статьи</a></li>
      <li class="yellow"><a rel="show_mag_interview" href="">Интервью</a></li>
      <li class="red"><a rel="show_mag_news" href="">Новости Москвы</a></li>
    </ul>
  </div>
  
  <div class="magazine_tab">
    <div id="show_mag_all" class="mag_tabs">

    <% Staff::DB.jouranl_all.each do |all| %>
      <%= render "shared/main_#{all.class.name.downcase}", object: all%>
    <% end %>

      <div class="clear"></div>
    </div>
    
    <div id="show_mag_article" class="mag_tabs">
      <div class="magazine_cell">
      <% Article.for_main.each do |article| %>
        <div class="preview_cell">
          <div class="info">
            <div class="cat_name"><span>Статьи</span></div>
            <div class="thumb"><%= link_to image_tag(article.picture.url), article_path(article) %></div>
            <div class="name"><%= link_to article.title, article_path(article) %></div>
            <div class="date"><%= article.created_at.to_s(:main) %></div>
          </div>
        </div>
      <% end %>
        <div class="clear"></div>
      </div>
    </div>
  
    <div id="show_mag_interview" class="mag_tabs">
      <div class="magazine_cell">
      <% Interview.for_main.each do |interview| %>
        <div class="preview_cell">
          <div class="info">
            <div class="cat_name"><span>Интервью</span></div>
            <div class="thumb"><%= link_to image_tag(interview.picture.url), interview_path(interview) %></div>
            <div class="name"><%= link_to interview.title, interview_path(interview) %></div>
            <div class="date"><%= interview.created_at.to_s(:main) %></div>
          </div>
        </div>
      <% end %>
        <div class="clear"></div>
      </div>
    </div>

    <div id="show_mag_news" class="mag_tabs">
      <div class="magazine_cell">
      <% News.for_main.each do |novelty| %>
        <div class="preview_cell">
          <div class="info">
            <div class="cat_name"><span>Интервью</span></div>
            <div class="thumb"><%= link_to image_tag(novelty.picture.url), news_path(novelty) %></div>
            <div class="name"><%= link_to novelty.title, news_path(novelty) %></div>
            <div class="date"><%= novelty.created_at.to_s(:main) %></div>
          </div>
        </div>
      <% end %>
      </div>
    </div>
  </div>
</div>
